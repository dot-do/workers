{
  "name": "@dot-do/rpc",
  "version": "0.1.0",
  "description": "CapnWeb RPC server with OAuth authentication",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "typecheck": "tsc --noEmit",
    "build": "tsc && esbuild src/index.ts --bundle --format=esm --outfile=dist/index.js --external:cloudflare:*"
  },
  "dependencies": {
    "@dot-do/protocol-router": "workspace:^",
    "hono": "^4.7.3",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250106.0",
    "@types/node": "^22.10.5",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8",
    "wrangler": "^3.98.0"
  }
}

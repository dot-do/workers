{
  "name": "@do/do",
  "version": "1.0.0",
  "private": true,
  "description": "DO Worker - RPC proxy for all services",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250710.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.24.0"
  }
}

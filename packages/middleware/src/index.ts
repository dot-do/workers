/**
 * @dotdo/middleware - Middleware for Cloudflare Workers
 *
 * Provides:
 * - Auth middleware (WorkOS AuthKit)
 * - Rate limiting (Cloudflare Rate Limiting bindings)
 * - CORS handling
 * - Request logging
 */

export * from './auth'
export * from './rate-limit'

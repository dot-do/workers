# `$` Execute Functions

The `$` symbol allows universal execution of functions in Code, Markdown and URLs

## Data

### `get`

Retrieve any resource from the database:

```js
$.get('blog/hello-world')
```




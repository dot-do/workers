{
	"name": "cloudflare-api",
	"version": "1.0.0",
	"description": "Cloudflare API token integration worker for .do platform",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"test": "vitest run",
		"test:watch": "vitest",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"hono": "^4.3.0",
		"zod": "^3.23.0"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20240117.0",
		"@types/node": "^20.11.0",
		"typescript": "^5.3.3",
		"vitest": "^1.2.0",
		"wrangler": "^3.25.0"
	}
}

{
  "name": "@dot-do/queue",
  "version": "1.0.0",
  "description": "Queue Service - Background job processing with Cloudflare Queues",
  "main": "worker.ts",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest --coverage",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "types": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.7.12"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "@cloudflare/workers-types": "^4.20250130.0",
    "prettier": "^3.6.2",
    "typescript": "^5.5.2",
    "vitest": "^3.2.0",
    "wrangler": "^4.40.3"
  }
}
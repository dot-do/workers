{
  "name": "@dot-do/auth",
  "version": "1.0.0",
  "private": true,
  "description": "Authentication and authorization microservice with WorkOS, API keys, JWT sessions, and RBAC",
  "scripts": {
    "dev": "wrangler dev --port 3000",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.0.5",
    "@cloudflare/workers-types": "^4.20250710.0",
    "@workos-inc/node": "^7.58.0",
    "hono": "^4.8.4",
    "jose": "^6.0.11"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "vitest": "^3.2.0",
    "wrangler": "^4.40.3",
    "typescript": "^5.5.2"
  }
}

{
  "name": "things-service",
  "version": "1.0.0",
  "description": "Things service - CRUD operations for entities with relationships and AI enrichments",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.7.15",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251001.0",
    "@cloudflare/vitest-pool-workers": "^0.9.9",
    "typescript": "^5.7.3",
    "vitest": "^3.2.0",
    "wrangler": "^3.100.0"
  }
}

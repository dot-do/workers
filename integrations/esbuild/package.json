{
  "name": "@dotdo/esbuild",
  "version": "0.0.1",
  "description": "esbuild-wasm exposed as multi-transport RPC worker",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "esbuild-wasm": "^0.24.0",
    "@dotdo/rpc": "workspace:*"
  }
}

{
	"name": "workflows",
	"version": "0.0.0",
	"private": true,
	"scripts": {
    "do": "wrangler deploy --dispatch-namespace do",
		"deploy": "wrangler deploy",
		"start": "wrangler dev",
		"dev": "wrangler dev",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"typescript": "^5.0.4",
		"wrangler": "^4.24.3"
	}
}